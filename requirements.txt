# Rathnone — requirements (gateway v1)
#
# Pinned to exact versions for reproducible, fail-closed builds. Bump deliberately
# and re-verify the suite (pytest + console tsc) before committing a version change.

# Reuse the frozen governance spine from sovereign-agent-fleet (imported as a
# library via the fleet_overlay .pth; fleet lives in its own repo, untouched).
cryptography==50.0.0
pycryptodome==3.23.0  # verified Keccak_256 (Ethereum hash); secp256k1 sign/recover hand-rolled
pynacl==1.6.2
argon2-cffi==25.1.0

# Gateway API surface (F4: fresh Python service)
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1

# Testing / verification gate
pytest==9.1.1

# (Phase 5) Product console — Next.js, set up separately under ui/
